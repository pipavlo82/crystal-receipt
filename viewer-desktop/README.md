# ReceiptOS Viewer Desktop

Offline Electron wrapper for the existing static ReceiptOS Viewer.

## Commands

```bash
cd viewer-desktop
npm install
npm run dev
npm run dist
```

## Notes

- Reuses `../docs/artifact-viewer/index.html`
- Works offline
- No backend or upload path
- Keeps proof/viewer semantics unchanged
